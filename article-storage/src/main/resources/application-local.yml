server:
  port: 8082

spring:
  kafka:
    bootstrapServers: localhost:9092

app:
  resources:
    rss-extractor:
      url: http://localhost:8081

management:
  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces
